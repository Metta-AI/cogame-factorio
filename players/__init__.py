"""cogame-factorio player side: websocket harness + baseline policies.

Each policy is runnable as ``python -m players.<name>`` inside a policy
container; ``players.client`` holds the shared wire code.
"""
